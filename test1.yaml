---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: sample name
  namespace: custom
  annotations:
    kubernetes.io/ingress.class: "internal"
spec:
  rules:
  - host: "sample-host"
    http:
      paths:
      - path: /visualization/
        backend:
          serviceName: my-own-servicename
          servicePort: 80