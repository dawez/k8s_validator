---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: this is sample number 2
  namespace: custom
  annotations:
    kubernetes.io/ingress.class: "internal"
spec:
  rules:
  - host: "sample-host"
    http:
      paths:
      - path: /visualization/
        backend:
          serviceName: my-own-servicename2
          servicePort: 80
# just adding some comments at the end
